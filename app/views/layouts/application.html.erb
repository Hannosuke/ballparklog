<!DOCTYPE html>
<html>
  <head>
    <title>Ballparklog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
  </head>

  <body>
    <div class="header navbar navbar-expand-md bg-white border-bottom">
      <h1 class="navbar-brand mr-0 mb-0"><%= link_to("BallPark Log", "/", class:"noline logo") %></h1>
      
      <% if session[:user_id]%>
        <%= link_to("#{current_user.name}さん", user_path(current_user.id), class:"nav black-link noline")%>
        <%= link_to("ログアウト", logout_path, method: :delete, class:"black-link noline")%>
      <% else %>
        <%= link_to("新規登録", new_user_path, class:"black-link noline") %>
        <%= link_to("ログイン", login_path, class:"nav-link noline")%>
      <% end %>
    </div>
      <div class="notifications">
          <% flash.each do |key, value|%>
            <%= content_tag(:div, value, class: key)%>
          <% end %>
      </div>
      <%= yield %>
  </body>
  <footer>
    <div class="text-center pt-4 web-copyright">
      Copurigith ©︎ 2021 BallParkLog.All Rights Reserved.
    </div>
  </footer>
</html>
