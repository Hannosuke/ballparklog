こんにちは<%= @user.name %>さん！
===============================================

以下のリンクからパスワードの再設定ができます。
<%= link_to("パスワード変更", edit_users_password_url(@user.password_reset_token))%>
    
このメールに心当たりがない場合は無視してください。
上記リンクよりパスワード変更を行うまで、パスワードの再設定はなされません。